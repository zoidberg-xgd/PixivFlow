# base
.DS_Store
node_modules

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
logs/
**/*.log

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*~

# Xcode (iOS/macOS development)
*.xcworkspace
!*.xcworkspace/contents.xcworkspacedata
*.xcuserdata
*.xcuserstate
*.xccheckout
*.xcscmblueprint
xcshareddata/
xcuserdata/
.swiftpm/

# lock
yarn.lock
# package-lock.json - Keep it for CI/CD and reproducible builds

# build file
*.zip
/dist-offline
/dist-special-DelMemory
/dist-special-Hongye
/dist-special-wolun
/dist-special-cao
*special*

# 当把 dist 目录离线安装为浏览器扩展时，浏览器会自动创建 _metadata 目录。这个目录不需要上传和打包
_metadata

.eslintcache

# Standalone mode - User config files (NEVER commit these!)
config/standalone.config.json
config/standalone.config.simple.json
# Allow example files but block any that might contain real tokens
config/standalone.config.*.json
!config/standalone.config.example.json
!config/standalone.config.*example*.json
!config/standalone.config.novel-chinese.json
!config/standalone.config.ranking.json

# User data directories (NEVER commit these!)
data/
downloads/
logs/
*.db
*.db-shm
*.db-wal

# Environment variables (NEVER commit these!)
.env
.env.local
.env.*.local
.env.production
.env.development
*.env
!.env.example

# Temporary token files (NEVER commit these!)
.refresh_token
*.token
.tmp/
tmp/
temp/

# WebUI
# 前端已分离到独立仓库: https://github.com/zoidberg-xgd/pixivflow-webui
# Frontend has been moved to separate repository

# Backend build output
dist/

# Test coverage reports
coverage/
*.lcov

# Test files (non-MD files)
test-*.js
test-*.ts
test-*.sh
**/test-*.ts
**/test-*.js
# Note: TEST*.md files are handled in "Internal work documents" section below

# Temporary files
*.backup
*.tmp
*.temp
*.bak
/tmp/

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.Spotlight-V100
.Trashes
._*

# Security reports (may contain sensitive information)
# Note: docs/SECURITY.md is allowed (it's documentation, not a report)
*SECURITY*.md
!docs/SECURITY.md

# Internal work documents (refactoring plans, progress reports, analysis, etc.)
# These are temporary development documents and should not be committed
REFACTORING_PLAN.md
*REFACTORING*.md
!docs/**/*REFACTORING*.md
*MIGRATION*.md
!docs/**/*MIGRATION*.md
*PROGRESS*.md
!docs/**/*PROGRESS*.md
*STATUS*.md
!docs/**/*STATUS*.md
*PERFORMANCE*.md
!docs/**/*PERFORMANCE*.md
*COMPLETE*.md
!docs/**/*COMPLETE*.md

# Test and analysis reports (internal only)
*TEST*.md
!docs/**/*TEST*.md
*REPORT*.md
!docs/**/*REPORT*.md
*ANALYSIS*.md
!docs/**/*ANALYSIS*.md
*BENCHMARK*.md
!docs/**/*BENCHMARK*.md
*VERIFICATION*.md
!docs/**/*VERIFICATION*.md
*SUMMARY*.md
!docs/**/*SUMMARY*.md
*CLEANUP*.md
!docs/**/*CLEANUP*.md
# Note: SECURITY_CLEANUP*.md is already covered by *SECURITY*.md above

# Documentation rewrite and planning documents (internal only)
*REWRITE*.md
!docs/**/*REWRITE*.md
*PLAN*.md
!docs/**/*PLAN*.md
!docs/project/*PLAN*.md
*INDEX*.md
!docs/**/*INDEX*.md

# Quality and performance analysis (internal only)
*QUALITY*.md
!docs/**/*QUALITY*.md
*SETUP*.md
!docs/**/*SETUP*.md

# Token storage files (never commit these!)
.pixiv-refresh-token
*.pixiv-refresh-token
**/.pixiv-refresh-token
**/*.pixiv-refresh-token

# Any file containing actual tokens (not placeholders)
# Note: config/standalone.config.json should use YOUR_REFRESH_TOKEN placeholder

# Build artifacts and generated files
*.map
*.d.ts.map
*.js.map
*.tsbuildinfo
.cache/
.parcel-cache/
.next/
.nuxt/
.output/

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files (already covered above, but explicit)
.env.test
.env.production.local
.env.development.local
.env.local

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Python (if any Python scripts are used)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# IDE - JetBrains
.idea/
*.iml
*.iws
*.ipr
out/

# IDE - VSCode (keep settings but ignore workspace state)
.vscode/settings.json
.vscode/launch.json
.vscode/extensions.json
.vscode/*.code-workspace

# macOS
.AppleDouble
.LSOverride
Icon
.DocumentRevisions-V100
.fseventsd
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk