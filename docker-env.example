# ============================================================================
# PixivFlow Docker 环境变量配置示例
# ============================================================================
# 
# 使用方法：
# 1. 复制此文件为 .env: cp docker-env.example .env
# 2. 根据实际情况修改配置
# 3. docker-compose 会自动读取 .env 文件
#
# ============================================================================

# ============================================================================
# 时区配置
# ============================================================================
TZ=Asia/Shanghai

# ============================================================================
# Pixiv 账号配置（可选，建议在配置文件中设置）
# ============================================================================
# PIXIV_REFRESH_TOKEN=your_refresh_token_here
# PIXIV_CLIENT_ID=your_client_id_here
# PIXIV_CLIENT_SECRET=your_client_secret_here

# ============================================================================
# 存储路径配置（容器内路径）
# ============================================================================
PIXIV_DATABASE_PATH=/app/data/pixiv-downloader.db
PIXIV_DOWNLOAD_DIR=/app/downloads
PIXIV_ILLUSTRATION_DIR=/app/downloads/illustrations
PIXIV_NOVEL_DIR=/app/downloads/novels

# ============================================================================
# 日志配置
# ============================================================================
PIXIV_LOG_LEVEL=info
# 可选值: debug, info, warn, error

# ============================================================================
# 调度器配置
# ============================================================================
PIXIV_SCHEDULER_ENABLED=true

# ============================================================================
# WebUI 配置
# ============================================================================
WEBUI_PORT=3000
# WebUI 服务端口（宿主机端口）

# ============================================================================
# 代理配置
# ============================================================================
# 注意：如果代理在宿主机上，使用 host.docker.internal 访问
# macOS/Windows: 使用 host.docker.internal
# Linux: 可以使用 172.17.0.1 或 host.docker.internal

# HTTP/HTTPS 代理
# HTTP_PROXY=http://host.docker.internal:6152
# HTTPS_PROXY=http://host.docker.internal:6152

# SOCKS5 代理
# ALL_PROXY=socks5://host.docker.internal:6153

# 小写版本（某些工具使用）
# http_proxy=http://host.docker.internal:6152
# https_proxy=http://host.docker.internal:6152
# all_proxy=socks5://host.docker.internal:6153

# ============================================================================
# 其他配置
# ============================================================================
# 跳过容器内自动登录（容器内无法进行交互式登录）
# PIXIV_SKIP_AUTO_LOGIN=true

