/**
 * 特定下载脚本（已废弃）
 *
 * ⚠️ 注意：此脚本已不再需要！
 *
 * 现在可以通过配置文件 + 通用 download 命令来实现相同功能：
 *
 * 1. 使用示例配置文件：
 *    cp config/specific-download.example.json config/standalone.config.json
 *
 * 2. 运行通用下载命令：
 *    pixivflow download
 *    或
 *    npm run download
 *
 * 这样做的好处：
 * - 不需要单独的脚本
 * - 配置更灵活，可以随时修改
 * - 代码更简洁，维护成本更低
 *
 * 如果你仍然想使用此脚本（向后兼容），它会调用通用的 download 命令。
 */
export {};
//# sourceMappingURL=download-specific.d.ts.map